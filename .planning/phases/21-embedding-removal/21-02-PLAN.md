---
phase: 21-embedding-removal
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - src/search/semantic.ts
  - src/search/hybrid.ts
  - src/search/index.ts
  - src/cli/commands/search.ts
  - src/cli/commands/index-cmd.ts
  - src/mcp/tools/semantic.ts
  - src/mcp/server.ts
  - src/mcp/sync.ts
  - tests/embeddings/integration.test.ts
  - tests/embeddings/pipeline.test.ts
  - tests/embeddings/text.test.ts
  - tests/db/vec.test.ts
  - tests/search/hybrid.test.ts
  - tests/search/semantic.test.ts
  - tests/mcp/tools.test.ts
  - tests/mcp/server.test.ts
  - tests/cli/search.test.ts
  - tests/db/schema.test.ts
  - CLAUDE.md
  - skill/SKILL.md
autonomous: true
requirements: [CLEAN-04, CLEAN-05, CLEAN-06]

must_haves:
  truths:
    - "kb search uses FTS5 only -- no hybrid/vector code path"
    - "kb search --semantic produces unknown option error"
    - "kb index --embed produces unknown option error"
    - "kb_semantic MCP tool does not exist"
    - "All tests pass with no embedding test files remaining"
    - "Documentation reflects FTS5-only search"
  artifacts:
    - path: "src/search/index.ts"
      provides: "Search barrel with only searchText, findEntity, queryDependencies"
    - path: "src/cli/commands/search.ts"
      provides: "Search command calling searchText for default, no --semantic"
    - path: "src/mcp/server.ts"
      provides: "MCP server without semantic tool registration"
    - path: "CLAUDE.md"
      provides: "Updated docs without embedding/semantic references"
    - path: "skill/SKILL.md"
      provides: "Skill docs without --semantic or embedding references"
  key_links:
    - from: "src/cli/commands/search.ts"
      to: "src/search/text.ts"
      via: "searchText import for default search"
      pattern: "searchText"
    - from: "src/mcp/server.ts"
      to: "src/mcp/tools/search.ts"
      via: "registerSearchTool (not registerSemanticTool)"
      pattern: "registerSearchTool"
---

<objective>
Remove all search-layer embedding consumers: semantic/hybrid search, CLI flags (--semantic, --embed), MCP semantic tool, related tests, and update documentation.

Purpose: Complete the embedding removal by cleaning up all consumers. Default search reverts to FTS5-only.
Output: Clean search layer, all tests pass, docs accurate.
</objective>

<execution_context>
@/Users/aleksander.winecki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aleksander.winecki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-embedding-removal/21-01-SUMMARY.md

<interfaces>
<!-- Key interfaces the executor needs from the existing codebase -->

From src/search/text.ts (FTS5 search -- this is what default search reverts to):
```typescript
export function searchText(db: Database.Database, query: string, options?: TextSearchOptions): TextSearchResult[];
```

From src/search/index.ts (current barrel -- must be trimmed):
```typescript
export { searchText } from './text.js';
export { searchSemantic } from './semantic.js';   // REMOVE
export { searchHybrid } from './hybrid.js';         // REMOVE
export { findEntity } from './entity.js';
export { queryDependencies, VALID_MECHANISMS } from './dependencies.js';
export { listAvailableTypes } from '../db/fts.js';
```

From src/mcp/sync.ts (withAutoSyncAsync only used by semantic tool):
```typescript
export async function withAutoSync<T>(...): Promise<T>;       // KEEP
export async function withAutoSyncAsync<T>(...): Promise<T>;   // REMOVE
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove search/CLI/MCP embedding code</name>
  <files>
    src/search/semantic.ts
    src/search/hybrid.ts
    src/search/index.ts
    src/cli/commands/search.ts
    src/cli/commands/index-cmd.ts
    src/mcp/tools/semantic.ts
    src/mcp/server.ts
    src/mcp/sync.ts
  </files>
  <action>
    1. Delete `src/search/semantic.ts` and `src/search/hybrid.ts`.

    2. In `src/search/index.ts`:
       - Remove `export { searchSemantic } from './semantic.js';`
       - Remove `export { searchHybrid } from './hybrid.js';`

    3. In `src/cli/commands/search.ts`:
       - Remove imports of `searchSemantic` and `searchHybrid` from `../../search/index.js`
       - Remove the `.option('--semantic', ...)` line
       - Remove the entire `--semantic` handler block (the `if (opts.semantic)` block, ~lines 62-74)
       - Replace the default hybrid search block with a direct FTS5 call. The current block uses `withDbAsync` + `searchHybrid`. Replace with `withDb` (sync) + `searchText`:
         ```typescript
         // Default: FTS5 text search
         withDb((db) => {
           const results = withTiming('search', () =>
             searchText(db, query, {
               limit: parseInt(opts.limit, 10),
               repoFilter: opts.repo,
               entityTypeFilter: opts.type,
             }),
           );
           output(results);
           if (opts.timing) reportTimings();
         });
         ```
       - The action function no longer needs to be `async` since all paths are sync. Remove `async` from the action callback.
       - Update imports: remove `withDbAsync` import (if no longer used), ensure `withDb` is imported. Remove `withTimingAsync` import, keep `withTiming`.

    4. In `src/cli/commands/index-cmd.ts`:
       - Remove the `.option('--embed', ...)` line
       - Remove `embed: opts.embed,` from the options object passed to `indexAllRepos`

    5. Delete `src/mcp/tools/semantic.ts`.

    6. In `src/mcp/server.ts`:
       - Remove `import { registerSemanticTool } from './tools/semantic.js';`
       - Remove `registerSemanticTool(server, db);` from `createServer()`

    7. In `src/mcp/sync.ts`:
       - Remove the `withAutoSyncAsync` function (keep `withAutoSync` which is used by other tools)
       - Remove the comment referencing "hybrid/semantic search in MCP tools" from above the deleted function

    8. Run `npm run build` to verify compilation.
  </action>
  <verify>
    <automated>npm run build 2>&1 && ! grep -rq "semantic\|hybrid\|searchHybrid\|searchSemantic\|withAutoSyncAsync\|embed" src/ --include="*.ts" --exclude-dir=embeddings && echo "PASS"</automated>
  </verify>
  <done>
    - src/search/semantic.ts and hybrid.ts deleted
    - search/index.ts only exports searchText, findEntity, queryDependencies, listAvailableTypes
    - CLI search uses searchText (sync) for default, no --semantic flag
    - CLI index has no --embed flag
    - MCP server has no semantic tool
    - sync.ts has no withAutoSyncAsync
    - Build compiles cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove embedding tests, update remaining tests and docs</name>
  <files>
    tests/embeddings/integration.test.ts
    tests/embeddings/pipeline.test.ts
    tests/embeddings/text.test.ts
    tests/db/vec.test.ts
    tests/search/hybrid.test.ts
    tests/search/semantic.test.ts
    tests/mcp/tools.test.ts
    tests/mcp/server.test.ts
    tests/cli/search.test.ts
    tests/db/schema.test.ts
    CLAUDE.md
    skill/SKILL.md
  </files>
  <action>
    1. Delete entire test files:
       - `tests/embeddings/integration.test.ts`
       - `tests/embeddings/pipeline.test.ts`
       - `tests/embeddings/text.test.ts`
       - `tests/db/vec.test.ts`
       - `tests/search/hybrid.test.ts`
       - `tests/search/semantic.test.ts`
       - Remove the `tests/embeddings/` directory itself (should be empty after deleting files)

    2. In `tests/mcp/tools.test.ts`:
       - Remove `vi.mock('../../src/search/hybrid.js', ...)` mock
       - Remove `vi.mock('../../src/search/semantic.js', ...)` mock
       - Remove `import { registerSemanticTool } from '../../src/mcp/tools/semantic.js';`
       - Remove `import { searchHybrid } from '../../src/search/hybrid.js';`
       - Remove the entire `describe('kb_semantic', ...)` test block
       - Remove `registerSemanticTool(server, db);` from any test setup
       - Remove `{ name: 'kb_semantic', args: { query: 'TestModule' } }` from any test arrays
       - Update `embeddings: 0` in IndexStats mock objects -- remove the `embeddings` field entirely from stats objects (since IndexStats no longer has it)

    3. In `tests/mcp/server.test.ts`:
       - Remove `'kb_semantic'` from the expected tool names array/assertion

    4. In `tests/cli/search.test.ts`:
       - Remove `vi.mock('../../src/search/semantic.js', ...)` mock
       - Remove `vi.mock('../../src/search/hybrid.js', ...)` mock
       - Remove `import { searchSemantic } from '../../src/search/semantic.js';`
       - Remove `import { searchHybrid } from '../../src/search/hybrid.js';`
       - Remove the entire `describe('CLI search --semantic', ...)` test block
       - Remove the entire `describe('CLI search default (hybrid)', ...)` test block
       - Add a new `describe('CLI search default (FTS5)', ...)` block that tests the default path calls `searchText` (the existing `searchText` mock and tests already cover this for the explicit query path, but the default non-entity non-semantic path should be verified)

    5. In `tests/db/schema.test.ts`:
       - Remove `import { loadVecExtension } from '../../src/db/vec.js';`
       - Remove or update the v8 migration test (`'v8 migration creates entity_embeddings vec0 table'`) -- delete this test entirely
       - Remove or update the `'fresh database has entity_embeddings table'` test -- update to assert entity_embeddings does NOT exist, or delete if redundant with other schema tests
       - Remove any `loadVecExtension` calls in test setup

    6. In `CLAUDE.md`:
       - Remove `- `kb search --semantic "natural language query"` -- Pure vector similarity search` line
       - Change `kb search "query"` description from "Hybrid FTS5 + semantic search (auto-degrades to FTS-only without embeddings)" to "Full-text search across indexed repos"
       - Remove `- `kb index --embed` -- Re-index with vector embedding generation (slower, enables semantic search)` line
       - Update test count from 561 to the actual count after test removal (run `npm test` to get exact count)

    7. In `skill/SKILL.md`:
       - Remove `--semantic -- pure vector similarity search (requires embeddings)` from the search refinement options
       - Remove any other references to embeddings or semantic search

    8. Run `npm test` to verify all tests pass and get final test count.
    9. Update CLAUDE.md with the correct test count from step 8.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5</automated>
  </verify>
  <done>
    - tests/embeddings/ directory gone
    - tests/db/vec.test.ts gone
    - tests/search/hybrid.test.ts and semantic.test.ts gone
    - tools.test.ts has no kb_semantic tests or hybrid/semantic mocks
    - server.test.ts does not expect kb_semantic tool
    - search.test.ts has no --semantic or hybrid tests
    - schema.test.ts has no v8/vec assertions
    - All remaining tests pass
    - CLAUDE.md and skill/SKILL.md reflect FTS5-only search, no embedding references, correct test count
  </done>
</task>

</tasks>

<verification>
- `ls src/search/semantic.ts src/search/hybrid.ts src/mcp/tools/semantic.ts 2>&1` all return "No such file"
- `ls tests/embeddings/ 2>&1` returns "No such file or directory"
- `ls tests/db/vec.test.ts tests/search/hybrid.test.ts tests/search/semantic.test.ts 2>&1` all return "No such file"
- `grep -r "semantic\|hybrid\|embed\|vec0\|sqlite-vec\|@huggingface" src/ tests/ --include="*.ts"` returns no matches
- `npm test` passes with 0 failures
- `npm run build` succeeds
- `grep -c "semantic" CLAUDE.md skill/SKILL.md` returns 0 for both
</verification>

<success_criteria>
All embedding consumers removed from search, CLI, MCP, and tests. Default search is FTS5-only. All tests pass. Docs are accurate. No references to embeddings, semantic search, hybrid search, sqlite-vec, or transformers.js remain anywhere in src/ or tests/.
</success_criteria>

<output>
After completion, create `.planning/phases/21-embedding-removal/21-02-SUMMARY.md`
</output>
