---
phase: 22-fixes-metadata
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - CLAUDE.md
autonomous: true
requirements: [META-01]

must_haves:
  truths:
    - "PROJECT.md tech stack has no sqlite-vec or transformers.js references"
    - "PROJECT.md lists 9 MCP tools (not 10) with no kb_semantic"
    - "PROJECT.md CLI flags have no --embed or --semantic"
    - "PROJECT.md test count reflects current reality (503)"
    - "CLAUDE.md test count is correct"
    - "Active requirements in PROJECT.md reflect Phase 21 completion"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Accurate project metadata"
    - path: "CLAUDE.md"
      provides: "Accurate test count and CLI documentation"
  key_links: []
---

<objective>
Update PROJECT.md and CLAUDE.md to accurately reflect the post-cleanup state of the codebase: no embedding dependencies, correct MCP tool count (9), correct CLI flags, correct test count (503), and updated active requirements.

Purpose: Project metadata must be truthful so future planning phases (and AI agents reading the docs) don't make decisions based on stale information.
Output: Updated PROJECT.md and CLAUDE.md with accurate stats.
</objective>

<execution_context>
@/Users/aleksander.winecki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aleksander.winecki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PROJECT.md metadata to reflect post-cleanup reality</name>
  <files>.planning/PROJECT.md</files>
  <action>
Read `.planning/PROJECT.md` and make the following targeted updates:

**Current State section (line 9-10):**
- Update "Latest shipped" to reflect v2.1 Phase 21 completion

**Current Milestone section:**
- Remove "Remove embedding infrastructure" from target features (it's done)
- Update to reflect Phase 22 is now the active work

**Active Requirements section:**
- Mark CLEAN-01 through CLEAN-06 as complete (move to Validated or mark with [x])
- Keep FIX-01, FIX-02, META-01 as active

**Context section (line 63):**
- Change "561 tests" to "503 tests" and "38 test files" to "32 test files"
- Remove `sqlite-vec` and `@huggingface/transformers` from tech stack
- Change `10 MCP tools` to `9 MCP tools`
- Remove `kb_semantic` from MCP tools list
- Remove `--embed` and `--semantic` from CLI flags list
- Remove `--force` from `kb index` flags (it still exists but `--repo` implies force — keep `--force` only if it's still meaningful for full reindex; actually keep it since `--force` is still a valid flag for full reindex)
- Keep `--force` in the CLI flags but remove `--embed` and `--semantic`

**Known limitations section:**
- Remove the bullet about embedding infrastructure being impractical (it's been removed, not just impractical)
  </action>
  <verify>
    <automated>grep -c "sqlite-vec\|transformers\|kb_semantic\|--semantic\|--embed\|561 tests" .planning/PROJECT.md | grep "^0$" || echo "STALE REFERENCES FOUND"</automated>
  </verify>
  <done>
    - No references to sqlite-vec, transformers.js, kb_semantic, --semantic, or --embed in PROJECT.md
    - MCP tool count is 9
    - Test count is 503
    - Active requirements section reflects CLEAN-* completion
    - Tech stack is accurate
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md test count and CLI docs</name>
  <files>CLAUDE.md</files>
  <action>
Read `CLAUDE.md` and verify/update:

1. **Test count** (line 58): Should say "503 tests" — verify it's already correct (Phase 21-02 updated this). If already correct, no change needed.

2. **CLI command list**: Verify no `--semantic` or `--embed` references remain. Phase 21-02 cleaned these. If already clean, no change needed.

3. **`kb index --force` line**: Keep as-is — `--force` is still valid for full reindex. After Plan 22-01 ships, `--repo` will imply force, but the `--force` flag itself still exists.

This task is primarily a verification pass. Only modify if stale references are found.
  </action>
  <verify>
    <automated>grep -c "semantic\|--embed\|transformers\|sqlite-vec" CLAUDE.md | grep "^0$" || echo "STALE REFERENCES FOUND"</automated>
  </verify>
  <done>
    - CLAUDE.md has correct test count
    - No stale embedding/semantic references
    - CLI docs are accurate
  </done>
</task>

</tasks>

<verification>
```bash
# No stale references in project metadata
grep -c "sqlite-vec\|transformers\|kb_semantic\|--semantic\|--embed" .planning/PROJECT.md CLAUDE.md

# Verify test count mentions are accurate
grep -n "503\|tests" .planning/PROJECT.md CLAUDE.md
```
</verification>

<success_criteria>
- PROJECT.md tech stack: no sqlite-vec, no @huggingface/transformers
- PROJECT.md tools: 9 MCP tools, no kb_semantic
- PROJECT.md CLI: no --embed, no --semantic flags
- PROJECT.md stats: 503 tests, 32 test files
- PROJECT.md known limitations: no embedding bullet
- CLAUDE.md: accurate and clean
</success_criteria>

<output>
After completion, create `.planning/phases/22-fixes-metadata/22-02-SUMMARY.md`
</output>
