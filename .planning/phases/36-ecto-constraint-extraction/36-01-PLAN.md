---
phase: 36-ecto-constraint-extraction
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/indexer/elixir.ts
  - src/indexer/pipeline.ts
  - tests/indexer/elixir.test.ts
  - tests/indexer/fields.test.ts
autonomous: true
requirements:
  - FEXT-01
  - FEXT-02
  - FEXT-03
must_haves:
  truths:
    - "Module attributes declared with ~w(...)a sigil are resolved to field name lists"
    - "Module attributes declared with [:atom, :list] syntax are resolved to field name lists"
    - "validate_required(@attr) resolves the attribute reference and marks those fields as required"
    - "cast(x, y, [:field1, :field2]) extracts inline permitted fields"
    - "cast(x, y, @required ++ @optional) resolves attribute references from the third argument"
    - "Pipe-form cast (|> cast(params, ...)) is handled identically to direct-call form"
    - "Pipeline nullability: field in requiredFields = not nullable; field in cast but not required = nullable"
    - "parseElixirFile populates optionalFields and castFields on ElixirModule"
  artifacts:
    - path: "src/indexer/elixir.ts"
      provides: "resolveModuleAttributes, extractCastFields, updated extractRequiredFields, extended ElixirModule interface"
      exports: ["resolveModuleAttributes", "extractCastFields", "extractRequiredFields"]
    - path: "src/indexer/pipeline.ts"
      provides: "Combined nullability determination using requiredFields + optionalFields + castFields"
      contains: "requiredSet"
    - path: "tests/indexer/fields.test.ts"
      provides: "Tests for module attribute extraction, cast extraction, combined nullability"
    - path: "tests/indexer/elixir.test.ts"
      provides: "Tests for parseElixirFile populating optionalFields and castFields"
  key_links:
    - from: "src/indexer/elixir.ts"
      to: "src/indexer/pipeline.ts"
      via: "ElixirModule.optionalFields and ElixirModule.castFields consumed in ectoFields mapping"
      pattern: "mod\\.castFields|mod\\.optionalFields"
    - from: "src/indexer/elixir.ts:resolveModuleAttributes"
      to: "src/indexer/elixir.ts:extractRequiredFields"
      via: "Attribute map passed to extractRequiredFields to resolve @attr references"
      pattern: "resolveModuleAttributes"
    - from: "src/indexer/elixir.ts:resolveModuleAttributes"
      to: "src/indexer/elixir.ts:extractCastFields"
      via: "Attribute map passed to extractCastFields to resolve @attr references in cast/4 calls"
      pattern: "extractCastFields"
---

<objective>
Extract Ecto constraint signals from @required_fields, @optional_fields, and cast/4 calls to produce accurate field nullability.

Purpose: The current extractor only recognizes inline atom lists in validate_required() calls, silently producing zero results when modules use module attribute indirection (e.g., validate_required(@required_fields)). This is documented in the existing test suite as a known gap.

Output: Extended ElixirModule with optionalFields and castFields, updated pipeline nullability logic, full test coverage via TDD.
</objective>

<execution_context>
@/Users/aleksander.winecki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aleksander.winecki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-ecto-constraint-extraction/36-RESEARCH.md

@src/indexer/elixir.ts
@src/indexer/pipeline.ts
@tests/indexer/fields.test.ts
@tests/indexer/elixir.test.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/indexer/elixir.ts (current interface to extend):
```typescript
export interface ElixirModule {
  name: string;
  type: string;
  filePath: string;
  moduledoc: string | null;
  functions: string[];
  tableName: string | null;
  schemaFields: { name: string; type: string }[];
  associations: { kind: string; name: string; target: string }[];
  absintheTypes: { kind: string; name: string }[];
  grpcStubs: string[];
  requiredFields: string[];
  // NEW fields to add:
  // optionalFields: string[];
  // castFields: string[];
}

export function extractRequiredFields(moduleContent: string): Set<string>;
export function parseElixirFile(filePath: string, content: string): ElixirModule[];
```

From src/indexer/pipeline.ts (line 182-193, current nullability logic):
```typescript
const ectoFields: FieldData[] = elixirModules
  .filter(mod => mod.tableName)
  .flatMap(mod =>
    mod.schemaFields.map(f => ({
      parentType: 'ecto_schema' as const,
      parentName: mod.name,
      fieldName: f.name,
      fieldType: f.type,
      nullable: !mod.requiredFields.includes(f.name),  // <-- only source today
      sourceFile: mod.filePath,
    }))
  );
```

From src/indexer/writer.ts:
```typescript
export interface FieldData {
  parentType: 'ecto_schema' | 'proto_message' | 'graphql_type';
  parentName: string;
  fieldName: string;
  fieldType: string;
  nullable: boolean;
  sourceFile: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Module attribute resolution and cast extraction with tests</name>
  <files>src/indexer/elixir.ts, tests/indexer/fields.test.ts, tests/indexer/elixir.test.ts</files>
  <behavior>
    - resolveModuleAttributes extracts `@attr ~w(word list)a` into { attr: ["word", "list"] }
    - resolveModuleAttributes extracts `@attr [:atom1, :atom2]` into { attr: ["atom1", "atom2"] }
    - resolveModuleAttributes handles mixed forms in same module (some ~w, some [:atom])
    - resolveModuleAttributes ignores attributes that aren't field lists (e.g., `@moduledoc`, `@doc`)
    - extractRequiredFields with attribute map resolves `validate_required(@attr)` via the map
    - extractRequiredFields with attribute map still handles inline atom lists `validate_required([:name, :email])`
    - extractRequiredFields with attribute map handles pipe form `|> validate_required(@attr)`
    - extractRequiredFields unions results across multiple validate_required calls (attribute + inline)
    - extractCastFields extracts inline atom list `cast(x, y, [:field1, :field2])`
    - extractCastFields extracts pipe form `|> cast(params, [:field1, :field2])`
    - extractCastFields resolves `cast(x, y, @fields)` via attribute map
    - extractCastFields resolves concatenation `cast(x, y, @required ++ @optional)` — both attributes resolved
    - extractCastFields handles pipe form with attribute reference `|> cast(params, @fields)`
    - extractCastFields handles pipe form with concatenation `|> cast(params, @required ++ @optional)`
    - parseElixirFile populates optionalFields from @optional_fields or equivalent (field list NOT used in validate_required)
    - parseElixirFile populates castFields from cast/4 calls
    - parseElixirFile populates requiredFields including attribute-resolved fields from validate_required(@attr)
    - Existing test "returns empty set for variable references like @required_fields" is updated: now returns the resolved fields
  </behavior>
  <action>
RED phase: Write failing tests in tests/indexer/fields.test.ts and tests/indexer/elixir.test.ts covering all behaviors above. Organize new tests in describe blocks:
- "Module attribute resolution" in fields.test.ts — tests for resolveModuleAttributes
- "Cast field extraction" in fields.test.ts — tests for extractCastFields
- "Attribute-aware required fields" in fields.test.ts — tests for updated extractRequiredFields with attribute resolution
- "parseElixirFile module attribute integration" in elixir.test.ts — tests for optionalFields and castFields on ElixirModule

GREEN phase: Implement in src/indexer/elixir.ts:

1. Add `optionalFields: string[]` and `castFields: string[]` to ElixirModule interface.

2. Create exported `resolveModuleAttributes(moduleContent: string): Record<string, string[]>` that:
   - Matches `@(\w+)\s+~w\(([\s\S]*?)\)a` for sigil form — split content on whitespace
   - Matches `@(\w+)\s+\[([\s\S]*?)\]` for atom list form — extract :atom names
   - Skips well-known non-field attributes: moduledoc, doc, derive, behaviour, behavior, impl, type, typep, spec, callback, optional_callbacks, compile, external_resource, dialyzer, on_definition, before_compile, after_compile, on_load, vsn
   - Returns map of attribute name to field name array

3. Update `extractRequiredFields` signature to accept optional `attrs?: Record<string, string[]>`. After the existing inline-list regex pass, add a second pass:
   - Match `validate_required\s*\(\s*(?:\w+\s*,\s*)?@(\w+)\s*\)` (direct call with attr ref)
   - Match `\|>\s*validate_required\s*\(\s*@(\w+)\s*\)` (pipe form with attr ref)
   - For each matched @attr, look up in attrs map and add all resolved fields to the Set
   - This is additive — the existing inline regex pass stays untouched

4. Create exported `extractCastFields(moduleContent: string, attrs: Record<string, string[]>): Set<string>` that:
   - Matches inline atom list in cast: both direct `cast(x, y, [:f1, :f2])` and pipe `|> cast(params, [:f1, :f2])`
   - Matches attribute reference in cast: both direct `cast(x, y, @attr)` and pipe `|> cast(params, @attr)`
   - Matches concatenation: `cast(x, y, @req ++ @opt)` — extracts ALL @attr references from third arg, resolves each via attrs map
   - Returns Set of all permitted field names

5. Create private `resolveOptionalFields(moduleContent: string, attrs: Record<string, string[]>, requiredFields: Set<string>, castFields: Set<string>): string[]` that:
   - Determines which module attributes are "optional" by semantic analysis:
     a. Any attribute whose name contains "optional" and is used in cast but not validate_required
     b. Any attribute used in cast (but NOT in validate_required) that doesn't contain "required" in its name
   - Returns the field names from those attributes
   - Fallback: fields in castFields that are NOT in requiredFields are also optional (even without explicit attribute)

6. Update `parseElixirFile` to:
   - Call `resolveModuleAttributes(moduleContent)` once per module
   - Pass attrs map to `extractRequiredFields(moduleContent, attrs)`
   - Call `extractCastFields(moduleContent, attrs)`
   - Compute optionalFields from cast-only fields (in cast but not in required)
   - Populate `optionalFields` and `castFields` on the ElixirModule output

7. Update the existing test "returns empty set for variable references like @required_fields": add a `@required_fields ~w(name email)a` declaration to the test fixture content, and expect the resolved Set to contain those fields.

REFACTOR phase: Ensure all new functions follow the existing style (regex-based, exported for testing, Set return type). Confirm `npm test` passes fully.
  </action>
  <verify>
    <automated>npx vitest run tests/indexer/elixir.test.ts tests/indexer/fields.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - resolveModuleAttributes handles both ~w(...)a and [:atom] forms
    - extractRequiredFields resolves @attr references in validate_required calls
    - extractCastFields extracts from inline lists and @attr references (including ++ concatenation)
    - parseElixirFile populates optionalFields and castFields on ElixirModule
    - All new tests pass, all existing tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pipeline nullability from combined signals</name>
  <files>src/indexer/pipeline.ts, tests/indexer/fields.test.ts</files>
  <behavior>
    - Field in requiredFields (from validate_required inline OR @attr resolution) -> nullable: false
    - Field in @optional_fields -> nullable: true
    - Field in cast but NOT in requiredFields -> nullable: true
    - Schema field with no changeset mention -> nullable: true (unchanged behavior)
    - Module with @required_fields ~w(name email)a + @optional_fields ~w(bio)a + cast using both: name/email not nullable, bio nullable
    - Module with only validate_required([:name]) + cast(attrs, [:name, :email]): name not nullable, email nullable (cast-only)
  </behavior>
  <action>
RED phase: Add new tests in tests/indexer/fields.test.ts under a "Combined nullability in pipeline" describe block. These are integration-level tests that verify the pipeline.ts ectoFields mapping produces correct nullable values given ElixirModules with various combinations of requiredFields, optionalFields, and castFields. Since the pipeline mapping is inline in extractRepoData (not a separate exported function), test via the ElixirModule interface contracts:
- Create test helper that simulates the pipeline nullability logic (the same expression: `!requiredSet.has(f.name)`)
- Test cases per behavior list above

GREEN phase: Update src/indexer/pipeline.ts lines 182-193 (the ectoFields mapping):

Before (line 190):
```typescript
nullable: !mod.requiredFields.includes(f.name),
```

After:
```typescript
nullable: !mod.requiredFields.includes(f.name),
```

Wait — `requiredFields` now includes attribute-resolved required fields (from Task 1's updated extractRequiredFields). The pipeline logic does NOT need to change if `requiredFields` already contains the full union of inline + attribute-resolved required fields. Verify this is the case.

However, the pipeline currently uses `.includes()` on an array. For correctness and performance, convert to a Set lookup:
```typescript
const requiredSet = new Set(mod.requiredFields);
// ...
nullable: !requiredSet.has(f.name),
```

This is a minimal change — the heavy lifting was done in Task 1 by making extractRequiredFields resolve attributes. The pipeline just consumes the enriched requiredFields array.

REFACTOR: Confirm `npm test` passes fully. The key verification is that the pipeline's nullable field now correctly reflects attribute-derived required fields.
  </action>
  <verify>
    <automated>npx vitest run tests/indexer/fields.test.ts --reporter=verbose && npm test</automated>
  </verify>
  <done>
    - Pipeline ectoFields mapping uses Set-based requiredFields lookup
    - Fields from @required_fields attribute produce nullable: false in pipeline output
    - Fields from @optional_fields or cast-only produce nullable: true
    - Full test suite (npm test) passes with zero regressions
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/indexer/elixir.test.ts tests/indexer/fields.test.ts --reporter=verbose` — all new + existing tests green
2. `npm test` — full suite (789+ tests) passes with zero regressions
3. `npm run build` — TypeScript compiles cleanly with new interface fields
</verification>

<success_criteria>
- ElixirModule interface has optionalFields and castFields arrays
- resolveModuleAttributes handles ~w(...)a and [:atom] forms generically (not hardcoded attribute names)
- extractRequiredFields resolves @attr references in validate_required calls
- extractCastFields extracts from inline lists and @attr references (including ++ concatenation)
- Pipeline nullable determination uses enriched requiredFields (no separate pipeline logic change needed beyond Set conversion)
- All 789+ tests pass, new tests cover each Ecto pattern from research (A through E)
- Existing test "returns empty set for variable references" is updated to verify resolution works
</success_criteria>

<output>
After completion, create `.planning/phases/36-ecto-constraint-extraction/36-01-SUMMARY.md`
</output>
